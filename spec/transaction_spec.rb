require "transaction"

describe Transaction do

  subject { described_class.new }
  let(:account) { double :account }
  # let(:start_station) {double :start_station }

  # it 'initialises after a deposit in specified format' do
  #   expect(subject).to include(date: [amount, :debit])
  # end

end
